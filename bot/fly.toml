app = "farmclaw-discord-bot"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[restart]
  policy = "always"

[http_service]
  internal_port = 8080
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
